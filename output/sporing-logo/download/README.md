# 스포링 로고·파비콘

- sporing-logo.png: 투명 배경 가로형 로고
- sporing-symbol.png: 투명 배경 S+R 심벌 원본
- favicon.ico: 16/32/48/256px 포함
- favicon-{size}.png: 16/32/48/180/192/256/512px

밝은 배경에 사용하는 로고입니다. PNG 래스터 파일이며 SVG 벡터 파일은 포함하지 않습니다.

내장 image_gen으로 승인된 S+R 시안을 분리 제작하고 파비콘 크기·포맷을 변환했습니다.

## 생성 프롬프트

로고: Edit this approved brand artwork without redesigning its geometry, letterforms or colors. Extract ONLY the top horizontal full logo lockup: blue/teal SR symbol plus navy Korean 스포링 and subtitle Sports + Ring. Remove bottom examples. Output single isolated logo, tight useful margins, wide landscape canvas. Background must be truly transparent alpha, no white background, no checkerboard drawn into pixels, no shadow or glow. Clean crisp production PNG asset.

심벌: Edit this approved brand artwork without redesigning its geometry, letterforms or colors. Extract ONLY the bottom left blue/teal SR symbol. No lettering or subtitle, no other examples. Center and enlarge the single symbol on square canvas, about 85% of canvas width. This is a website favicon. Background must be truly transparent alpha, no white background, no checkerboard drawn into pixels, no shadow or glow. Clean crisp production PNG asset.
